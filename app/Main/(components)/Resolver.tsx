import {ResolverProps} from "../../(root)/fronendTypes"
import React from 'react';
const Resolver = ({ resQL }: ResolverProps): JSX.Element => {
  return (
    <p className="Resolver">
      <code>put cool functions here</code>
    </p>
  );
};

export default Resolver;
