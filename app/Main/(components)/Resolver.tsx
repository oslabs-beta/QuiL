const Resolver = ({ uri }) => {
  return (
    <p className="Resolver">
      <blockquote>
        <pre>
          <code>{uri} Testing code </code>
        </pre>
      </blockquote>
    </p>
  );
};

export default Resolver;
